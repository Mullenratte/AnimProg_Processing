class BoundingBox {
  PVector position;
  float boxWidth;
  float boxHeight;
  
  public BoundingBox(PVector pos, float w, float h){
    this.position = pos;
    this.boxWidth = w;
    this.boxHeight = h;
  }
  
  
}
